version: '3.5'

services:
  lumen:
    ports:
      - "8000:8000"

    image: gabrielsoaresdev/lumen-test:latest
    restart: always


volumes:
   lumen-test-volume:
    name: lumen-test-volume
networks:

  lumen-network:
    driver: bridge
